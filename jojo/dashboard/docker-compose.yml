services:
  glance:
    container_name: glance
    image: glanceapp/glance
    restart: unless-stopped
    ports:
      - 8080:8080
    env_file: .env
    environment:
      - PROXMOXVE_URL=${PROXMOXVE_URL}
      - PROXMOXVE_KEY=${PROXMOXVE_KEY}
      - GITHUB_NOTIF_TOKEN=${GITHUB_NOTIF_TOKEN}
      - GITHUB_REPO_TOKEN=${GITHUB_REPO_TOKEN}
      - GH_REPO_1=${GH_REPO_1}
      - GH_REPO_2=${GH_REPO_2}
      - GH_REPO_3=${GH_REPO_3}
      - TAILSCALE_API_KEY=${TAILSCALE_API_KEY}
    volumes:
      - /docker/glance/config:/app/config
      - /docker/glance/assets:/app/assets
      - /etc/localtime:/etc/localtime:ro
      - /var/run/docker.sock:/var/run/docker.sock:ro
